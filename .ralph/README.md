# 🤖 Enterprise Ralph — Автономный AI-Агент Разработки

## Философия

**Enterprise Ralph** — это двухуровневая агентная система для автономной разработки крупных проектов.

### Три Главных Принципа:

1. **Полная Амнезия** — Агент перезапускается каждый раз заново. Он не помнит прошлый диалог, он видит только текущее состояние файлов.

2. **Жесткие Рельсы (Guardrails)** — Агент не имеет права сказать «я закончил», пока тесты не пройдут.

3. **Единый Источник Правды** — Файл `PRD.md` управляет всем.

---

## 📁 Структура Файлов

```
.ralph/
├── README.md                    # Этот файл
├── ralph.sh                     # Главный оркестратор (Entry Point)
├── architect.sh                 # Архитектор — планирует задачи
├── worker.sh                    # Рабочий — выполняет шаги плана
│
├── prompts/
│   ├── system_prompt.md         # Базовые инструкции для агента
│   ├── architect_prompt.md      # Промпт для режима Архитектора
│   └── worker_prompt.md         # Промпт для режима Рабочего
│
├── context/
│   ├── tech_stack.md            # Технологический стек проекта
│   ├── coding_standards.md      # Стандарты кодирования
│   └── architecture.md          # Архитектура проекта
│
├── state/
│   ├── current_plan.md          # Текущий план от Архитектора
│   ├── focus_list.txt           # Файлы, которые агент запросил
│   ├── error.log                # Последняя ошибка тестов
│   ├── error_history.log        # История ошибок для анти-залипания
│   └── iteration_count.txt      # Счётчик итераций
│
├── memory/
│   ├── decisions.md             # Архитектурные решения
│   ├── lessons_learned.md       # Уроки и ошибки
│   └── conventions.md           # Соглашения по коду
│
├── logs/
│   └── activity.log             # Лог всех итераций
│
└── scripts/
    ├── build_context.sh         # Сбор умного контекста
    ├── run_lint.sh              # Быстрый линтинг
    ├── run_tests.sh             # Запуск тестов
    └── check_completion.sh      # Проверка завершения
```

---

## 🚀 Быстрый Старт

```bash
# 1. Перейти в корень проекта
cd /Users/alsmolentsev/tms_new

# 2. Запустить Ralph
./.ralph/ralph.sh

# 3. Только планирование (Архитектор)
./.ralph/architect.sh

# 4. Только выполнение (Рабочий)
./.ralph/worker.sh
```

---

## 📝 PRD.md — Product Requirements Document

Все задачи управляются через файл `PRD.md` в корне проекта:

```markdown
# План разработки

## Epic 1: Авторизация
- [x] Настроить базовый проект
- [ ] Создать API регистрации
- [ ] Реализовать валидацию

## Epic 2: Личный кабинет
- [ ] Создать компонент Layout
```

---

## 🔄 Режимы Работы

### 1. **Автономный режим** (`ralph.sh`)
Полный цикл: Архитектор планирует → Рабочий выполняет → Тесты → Коммит

### 2. **Только планирование** (`architect.sh`)
Архитектор анализирует задачу и создаёт детальный план в `state/current_plan.md`

### 3. **Только выполнение** (`worker.sh`)
Рабочий выполняет шаги из `state/current_plan.md`

---

## 🛡️ Механизмы Защиты

### Анти-Залипание (Error Reflection)
Если тесты падают 3 раза подряд с одной ошибкой:
- Агент переходит в режим DEBUG
- Вместо кода генерирует анализ и гипотезы
- Сбрасывает подход и пробует заново

### Линтинг как Первая Линия
Перед тяжёлыми тестами запускается быстрый синтаксический чек.
Это экономит время на исправление опечаток.

### Память Проекта
Агент ведёт файлы в `memory/`:
- `decisions.md` — архитектурные решения
- `lessons_learned.md` — что пошло не так
- `conventions.md` — соглашения (имена, библиотеки)

---

## ⚙️ Конфигурация

Настройки в начале `ralph.sh`:

```bash
MAX_ITERATIONS=50        # Максимум итераций
ERROR_THRESHOLD=3        # Порог для анти-залипания
LINT_FIRST=true          # Линтинг перед тестами
AUTO_COMMIT=true         # Авто-коммит при успехе
ARCHITECT_INTERVAL=5     # Вызов Архитектора каждые N итераций
```

---

## 📊 Логирование

Все действия записываются в `logs/activity.log`:

```
2026-01-18 01:00:00 | [START] Ralph Loop начат
2026-01-18 01:00:05 | [ARCHITECT] Планирование задачи: API регистрации
2026-01-18 01:00:30 | [WORKER] Итерация #1: Создание файла auth.ts
2026-01-18 01:01:00 | [LINT] ✅ Синтаксис OK
2026-01-18 01:01:30 | [TEST] ❌ Тесты упали (1/3)
2026-01-18 01:02:00 | [WORKER] Итерация #2: Исправление ошибки
2026-01-18 01:02:30 | [TEST] ✅ Тесты пройдены
2026-01-18 01:02:35 | [COMMIT] feat(auto): Реализация API регистрации
```
