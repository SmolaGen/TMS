# 🤖 Enterprise Ralph v2.0 — Автономный AI-Агент Разработки

**Enterprise Ralph** — это мощная агентная система для автономного программирования, интегрированная в проект TMS.

## 🚀 Основные улучшения v2.0

- **Безопасная Песочница**: Все команды запускаются через `docker run` в изолированной среде.
- **Модульность**: Прозрачный код на Python вместо сложных bash-скриптов.
- **Guardrails**: Защита от удаления критического кода и повреждения файлов.
- **Устойчивость к сбоям**: Автоматические ретраи при работе с LLM API.
- **Краткость и Ясность**: Новая система логов в формате Markdown таблицы.

---

## 📁 Структура папки .ralph/

- `ralph.py` — Главный оркестратор.
- `utils.py` — Безопасность и проверка файлов.
- `llm_client.py` — Клиент для работы с AI (VibeProxy).
- `ralph_config.json` — Настройки системы.
- `Dockerfile.sandbox` — Описание среды выполнения для агента.
- `LOG.md` — Интерактивный лог работы.
- `ARCHITECTURE.md` — Подробное описание внутренних механизмов.

---

## 🛠️ Как запускать

Для работы Ralph должен быть запущен **Docker**.

### Обычный запуск
Ralph сам найдет первую задачу в `PRD.md` и начнет её выполнять:
```bash
python3 .ralph/ralph.py
```

### С автоматическим подтверждением
```bash
python3 .ralph/ralph.py --auto
```

### Только планирование (Архитектор)
Если вы хотите, чтобы Ralph составил план, но не приступал к коду:
```bash
python3 .ralph/ralph.py --plan
```

---

## 🛡️ Правила безопасности (Guardrails)

1. **Защита файлов**: Ralph не даст AI удалить более 50% кода в файле за раз или убрать важные настройки (например, конфиг FastAPI).
2. **Белый список команд**: Можно запускать только разрешенные инструменты (`pytest`, `npm`, `git` и т.д.).
3. **Бэкапы**: Перед любым изменением создается резервная копия в `.ralph/backups/`.

---

## 📝 Разработка через PRD.md

Ralph управляется через файл `PRD.md` в корне вашего проекта.
Просто добавьте новую задачу в формате `- [ ] Задача`, и при запуске Ralph возьмется за неё.
Когда задача выполнена, Ralph сам поставит отметку `[x]`.
