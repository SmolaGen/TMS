# Ralph Quick Start - Автономный режим "одна задача = один агент"

## 🎯 Концепция

Ralph теперь работает в режиме **непрерывной разработки**:

1. **Читает** первую незавершённую задачу из `PRD.md`
2. **Работает** над ней (максимум 20 итераций)
3. **Успех?** → Отмечает `[x]` → Перезапускается → Берёт следующую задачу
4. **Неудача?** → Записывает лог → Перезапускается → Пробует эту же задачу снова (новый агент читает опыт предыдущего)

## 🚀 Использование

### Шаг 1: Добавьте задачи в PRD.md

```bash
cat >> PRD.md << 'EOF'

## Epic 5: API Improvements
- [ ] Добавить healthcheck эндпоинт GET /health
- [ ] Добавить metrics эндпоинт GET /metrics
- [ ] Написать тесты для новых эндпоинтов
EOF
```

### Шаг 2: Запустите Ralph

```bash
# Из корня проекта
./ralph
```

**Или:**

```bash
python3 .ralph/ralph.py
```

### Шаг 3: Наблюдайте

Ralph будет:
- 📋 Брать задачу "Добавить healthcheck эндпоинт..."
- 🔨 Создавать код
- 🧪 Запускать тесты
- ✅ Если тесты прошли → отметит задачу [x] → возьмёт следующую
- ❌ Если не вышло за 20 итераций → перезапустится с новым агентом (который читает что пробовал предыдущий)

## 📊 Мониторинг

### Логи

```bash
# Смотреть логи в реальном времени
tail -f .ralph/LOG.md

# Просмотреть всю историю
less .ralph/LOG.md
```

### Текущая задача

```bash
# Найти первую невыполненную задачу
grep -n "^\s*-\s*\[ \]" PRD.md | head -1
```

### Прогресс

```bash
# Сколько выполнено
grep -c "^\s*-\s*\[x\]" PRD.md

# Сколько осталось
grep -c "^\s*-\s*\[ \]" PRD.md
```

## 🛑 Остановка

```bash
# Ctrl+C для остановки текущего агента
# Ralph остановится, но не потеряет прогресс
# При следующем запуске продолжит с той же задачи
```

## 🔧 Настройка

Отредактируйте `.ralph/ralph.py`:

```python
MAX_ITERATIONS = 20  # Сколько попыток на одного агента
MODEL = "gemini-2.5-flash"  # Или другая модель VibeProxy
```

## 💡 Советы

### 1. Формулируйте задачи чётко

❌ Плохо:
```markdown
- [ ] Улучшить производительность
```

✅ Хорошо:
```markdown
- [ ] Добавить кэширование списка водителей в Redis на 5 минут
```

### 2. Разбивайте крупные задачи

❌ Плохо:
```markdown
- [ ] Реализовать систему отчётов с экспортом в Excel, PDF и email рассылкой
```

✅ Хорошо:
```markdown
- [ ] Создать эндпоинт GET /api/v1/reports/daily для дневных отчётов
- [ ] Добавить экспорт отчёта в Excel формат
- [ ] Добавить экспорт отчёта в PDF формат
- [ ] Реализовать email рассылку отчётов
```

### 3. Добавляйте контекст в AGENTS.md

Ralph читает `AGENTS.md` для понимания стиля кода. Обновляйте его при изменении архитектуры.

## 📝 Примеры задач

### Backend (Python/FastAPI)

```markdown
- [ ] Добавить эндпоинт POST /api/v1/drivers для создания водителя
- [ ] Реализовать валидацию телефона водителя через regex
- [ ] Добавить поле telegram_username в модель Driver
- [ ] Оптимизировать запрос get_active_orders с JOIN вместо N+1
```

### Frontend (React/TypeScript)

```markdown
- [ ] Создать компонент DriverCard для отображения карточки водителя
- [ ] Добавить фильтрацию по статусу в компонент OrderList
- [ ] Реализовать dark mode в Layout компоненте
- [ ] Добавить валидацию формы CreateOrderForm с react-hook-form
```

### Тесты

```markdown
- [ ] Написать тест для создания заказа с пересекающимся временем
- [ ] Добавить integration тест для workflow: создание → назначение → завершение
- [ ] Покрыть тестами OrderService.calculate_route метод
```

### Документация

```markdown
- [ ] Обновить API.md с новыми эндпоинтами /health и /metrics
- [ ] Добавить примеры использования DriverService в docs/backend.md
- [ ] Написать migration guide для обновления с v1 на v2
```

## 🐛 Troubleshooting

### Ralph зациклился на одной задаче

**Проблема:** Агент перезапускается, но не может решить задачу.

**Решение 1:** Проверьте логи
```bash
tail -100 .ralph/LOG.md
```

Возможно задача слишком сложная или в коде критическая ошибка.

**Решение 2:** Упростите задачу
```markdown
# Было:
- [ ] Реализовать систему авторизации с JWT

# Стало:
- [ ] Создать модель User с полями email, password_hash
- [ ] Создать эндпоинт POST /auth/login
- [ ] Добавить генерацию JWT токена в AuthService
- [ ] Написать тесты для авторизации
```

**Решение 3:** Выполните вручную
```bash
# Отметьте задачу как выполненную
sed -i '' 's/- \[ \] Сложная задача/- [x] Сложная задача/' PRD.md

# Ralph перейдёт к следующей
```

### Ralph не находит PRD.md

```bash
# Создайте файл в корне проекта
cat > PRD.md << 'EOF'
# Product Requirements Document

## Epic 1: Setup
- [x] Создать PRD.md
- [ ] Добавить первую задачу
EOF
```

### Ошибка подключения к VibeProxy

```bash
# Проверьте что VibeProxy запущен
curl http://127.0.0.1:8317/v1/models

# Если нет, запустите
vibeproxy serve
```

## 🎓 Как это работает

1. **Запуск**: `./ralph` → `python3 .ralph/ralph.py`
2. **Читает** `PRD.md`, находит первую `- [ ]` задачу
3. **Создаёт агента** с промптом из `SYSTEM_PROMPT`
4. **Цикл** (до MAX_ITERATIONS):
   - Агент читает логи предыдущих попыток
   - Агент решает: писать код или запускать команду
   - Результат записывается в `.ralph/LOG.md`
5. **Успех?** → `mark_task_complete()` → `os.execv()` (перезапуск скрипта)
6. **Неудача?** → Лог записан → `os.execv()` (перезапуск с новым агентом)
7. **Повтор** с шага 2

## 🔮 Что дальше?

После того как Ralph выполнит все задачи из PRD.md:

```
🎉 All tasks completed! No unchecked tasks in PRD.md
```

Добавьте новые задачи и запустите снова:

```bash
echo "- [ ] Следующая задача" >> PRD.md
./ralph
```

---

**Готово! Запускайте и наблюдайте как Ralph работает. 🚀**
