version: '3.8'

services:
  # PostgreSQL с PostGIS для гео-данных
  postgis:
    image: postgis/postgis:16-3.4-alpine
    container_name: tms-postgis
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-tms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tms_secret}
      POSTGRES_DB: ${POSTGRES_DB:-tms_db}
    # ports:
    #   - "5433:5432"  # Закрыт для внешнего доступа (production)
    volumes:
      - postgis_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tms} -d ${POSTGRES_DB:-tms_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - tms-network

  # Redis для кэширования и брокера задач
  redis:
    image: redis:7-alpine
    container_name: tms-redis
    restart: unless-stopped
    # ports:
    #   - "6379:6379"  # Закрыт для внешнего доступа (production)
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - tms-network

  # OSRM для маршрутизации (Приморский край + Якутия)
  osrm:
    image: osrm/osrm-backend:latest
    container_name: tms-osrm
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./osrm-data:/data
    command: osrm-routed --algorithm mld /data/far-eastern-fed-district-latest.osrm
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - tms-network
    profiles:
      - routing

  # Photon для геокодинга
  photon:
    image: rtuszik/photon-docker:latest
    container_name: tms-photon
    restart: unless-stopped
    ports:
      - "2322:2322"
    volumes:
      - photon_data:/photon/photon_data
    environment:
      - JAVA_OPTS=-Xms512m -Xmx2g
      - REGION=russia
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:2322/api?q=test"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
    networks:
      - tms-network
    profiles:
      - geocoding

  # FastAPI Backend
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tms-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-tms}:${POSTGRES_PASSWORD:-tms_secret}@postgis:5432/${POSTGRES_DB:-tms_db}
      - REDIS_URL=redis://redis:6379/0
      - OSRM_URL=http://osrm:5000
      - PHOTON_URL=http://photon:2322
    volumes:
      - ./src:/app/src
      - ./alembic:/app/alembic
    depends_on:
      postgis:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - tms-network

  # Ingest Worker
  ingest-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tms-ingest-worker
    restart: unless-stopped
    command: python -m src.workers.ingest_worker
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-tms}:${POSTGRES_PASSWORD:-tms_secret}@postgis:5432/${POSTGRES_DB:-tms_db}
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - ./src:/app/src
    depends_on:
      postgis:
        condition: service_healthy
      redis:
        condition: service_healthy
    deploy:
      replicas: 1
    healthcheck:
      disable: true
    networks:
      - tms-network

  # Nginx Reverse Proxy
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: tms-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend/dist:/usr/share/nginx/html:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./certbot/www:/var/www/certbot:ro
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - tms-network
    profiles:
      - production

networks:
  tms-network:
    driver: bridge

volumes:
  postgis_data:
  redis_data:
  photon_data:
